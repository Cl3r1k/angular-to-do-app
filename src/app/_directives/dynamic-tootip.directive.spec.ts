import { DynamicTootipDirective } from './dynamic-tootip.directive';

describe('DynamicTootipDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTootipDirective();
    expect(directive).toBeTruthy();
  });
});
